/*import axios from "axios";
import http from "../http-common";


/*const get = id => {
  return http.get(`//${id}`);
};


const update = (id, data) => {
  return http.put(`//${id}`, data);
};*/
/*
const remove = id => {
  return http.delete(`/delete_member/${id}`);
};

const removeAll = () => {
  return http.delete(`/delete_members`);
};

export function updateUser(user,UserId){
    return axios.put(`https://pluma.ai/api/update`/${userId}.json,
    userD
    )
}
const UserService = {
  
 // get,

  //update,
  
};

export default UserService;*/